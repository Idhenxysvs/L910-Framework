console.log(`
🚀 ЗАПУСК ДВУХ СЕРВЕРОВ
=========================
`);

// Запускаем хаб (порт 3000)
console.log('1. Запускаем главный хаб (порт 3000)...');
require('./server/server.js');

// Ждем 1 секунду и запускаем концерты
setTimeout(() => {
    console.log('2. Запускаем концерты (порт 3001)...');
    require('./departments/concerts/src/server.js');
}, 1000);

// Выводим информацию
setTimeout(() => {
    console.log(`
✅ ОБА СЕРВЕРА ЗАПУЩЕНЫ:

🏠 Главный хаб:    http://localhost:3000
🎵 Концерты:       http://localhost:3001

=========================
⚡ Для остановки нажмите Ctrl+C дважды
    `);
}, 2000);